--compatability: full, in effect
--tested: yes

document = {
    changed = false,
    markChanged = function() --i dont think this is something im prioritising
        -- document.changed	= true
        -- PCspire.setTitle("*PCspire - " .. PCspire.scripttitle)
    end,
}